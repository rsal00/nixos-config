 ## NixOS configuration repository

# Machines
- `nanou`: Mac Mini late 2012
- `flim': Lenovo Thinkpad

# Structure
- `hosts/`: Machine-specific configurations
- `dotfiles/`: Application configs (Hyprland, Waybar, ...)
- `flake.nix`: Main flake entry point
